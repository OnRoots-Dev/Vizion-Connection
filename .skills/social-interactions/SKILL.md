# Social Interactions Skill

Cheer means encouragement, not a generic Like. Connection is consent-based mutual relationship, not a Follow clone. Comments are contextual and moderated. Every interaction needs actor/target authorization, deduplication or idempotency where relevant, rate limiting, visibility checks, abuse controls, accessible feedback, and no leakage of private objects. Do not repurpose `user_follows` as Connection without a migration plan.
