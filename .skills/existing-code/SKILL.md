# Existing Code Skill

Before implementation, search `app/`, `components/`, `features/`, `lib/`, `supabase/migrations/`, and docs. Classify findings as Current/Planned/Deprecated. Reuse the closest canonical component/service/API; do not duplicate responsibilities. Production DB is authoritative over migrations. Keep changes minimal and verify TypeScript, lint, targeted tests, and git diff.
